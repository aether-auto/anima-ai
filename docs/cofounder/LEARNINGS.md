<!--
  docs/cofounder/LEARNINGS.md — obstacles, mistakes, and challenges surfaced during
  cofounder:execute sprints. Append-only, one entry per completed task. This is distinct
  from DECISIONS.md: DECISIONS.md is what was decided and why; this file is what went
  wrong or was harder than expected while building it. Written by the execute-skill
  orchestrator after it reads each task's `learnings` field, never hand-edited directly.
-->

# Learnings

_Distilled obstacles, mistakes, and challenges from every completed cofounder:execute task._

<!--
  Entry format, one per task:

  ## [sprint N] task-id — task title
  **Date:** YYYY-MM-DD
  **Build engine:** opus | gpt-5.6-sol

  - Obstacle/mistake/note, one line each, taken directly from the task's `learnings` field.

  Keep entries short and specific — this file is read in full at the start of every
  sprint's Fable5 QA pass, so verbosity here has a real ongoing cost.
-->
