# Hash-locked build backend for the golden container's editable install.
# Installed with --require-hashes --no-deps before `pip install --no-build-isolation -e .`
# so the setuptools build backend never escapes the hash lock (closes the
# repo-scaffold-ci P2: build deps escaping the hash lock via build isolation).

setuptools==83.0.0 \
    --hash=sha256:29b23c360f22f414dc7336bb39178cc7bcbf6021ed2733cde173f09dba19abb3
wheel==0.47.0 \
    --hash=sha256:212281cab4dff978f6cedd499cd893e1f620791ca6ff7107cf270781e587eced
